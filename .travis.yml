# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: R

before_install:
  - cd pkg/gpcovr

sudo: false
dist: trusty

cache: packages

addons:
  apt:
    packages:
      - libgsl0-dev